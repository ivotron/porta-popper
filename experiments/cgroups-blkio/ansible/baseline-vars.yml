---
parameters:
  mode:
  - read
  - write
  - randread
  - randwrite

benchmarks:
- name: fio
  image: ivotron/fio
  entrypoint: genfio-test
  volumes:
  - /tmp/results:/results
  args: '-s -b 4k -d /dev/sdb -r 60 -m {{ params.mode }}'
  get:
  - /tmp/results
  devices:
  - /dev/sdb:/dev/sdb
