---
num_repetitions: 5
device: /dev/sdb
benchmarks:
- name: fio
  image: ivotron/fio
  entrypoint: genfio-test
  args: '-s -m rw -d {{ device }} -b 4k -r 30'
  volumes:
  - /tmp/results:/results
  fetch:
  - /tmp/results
  devices:
  - '{{ device }}:{{ device }}'
